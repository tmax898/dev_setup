return {
  "nvim-neo-tree/neo-tree.nvim",
  opts = {
    filesystem = {
      group_empty_dirs = true,
      follow_current_file = {
        enabled = true,
      },
    },
    buffers = {
      group_empty_dirs = true,
    },
    window = {
      auto_expand_width = true,
    },
  },
}
